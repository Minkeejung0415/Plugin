---
phase: 01-measurement-contract-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md
autonomous: true
requirements: [SD-01, SD-03, SD-04, SD-05, LOSS-01, STRESS-05, STALL-01, STALL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "SD-card data is the v1.1 ground-truth acquisition record; host/Open Ephys/UDP paths are comparison sinks only"
    - "The baseline contract requires per-sample seq, timestamp_us, and channel payload"
    - "The baseline contract requires session metadata sufficient to decode sample rate, channel count/layout, firmware mode, filter mode, SD mode, and stream mode"
    - "The baseline contract requires generated/saved/dropped/overrun/latency counters before no-loss claims are accepted"
    - "The baseline diagnosis explicitly names current per-sample fwrite+fflush as an acquisition-loop stall risk"
    - "The baseline diagnosis explicitly names s_latest as a non-lossless stream handoff"
    - "Phase 1 does not implement the buffered SD logger; that belongs to Phase 2"
  artifacts:
    - path: ".planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md"
      provides: "SD sample-log contract, firmware counter contract, mode matrix, and current failure-layer diagnosis"
      contains: "Ground Truth, File Contract, Counter Contract, Mode Matrix, Baseline Diagnosis, Verification Checklist"
---

<objective>
Create the Phase 1 baseline/contract artifact that turns the v1.1 reliability goal into a precise implementation contract. The artifact must define what the SD file must contain, what firmware counters are required, what modes must be testable, and which current code paths are known risk layers before Phase 2 changes firmware.

Purpose: prevent vague "no samples lost" claims by defining the exact evidence required.
Output: `.planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md`.
</objective>

<execution_context>
@C:\Users\justi\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-measurement-contract-baseline/01-CONTEXT.md
@.planning/phases/01-measurement-contract-baseline/01-RESEARCH.md

Relevant source files:
@esp32/firmware/main/main.c
@esp32/firmware/main/open_ephys_stream.h
@esp32/firmware/main/open_ephys_stream.c
@esp32/firmware/main/sd_logger.h
@esp32/firmware/main/sd_logger.c
@esp32/host/stress_test_serial.py
@esp32/host/analyze_sample_rate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the SD ground-truth file contract</name>
  <read_first>
    - `esp32/firmware/main/open_ephys_stream.h`
    - `esp32/firmware/main/main.c`
    - `01-CONTEXT.md` Ground-Truth Recording Contract
    - `01-RESEARCH.md` Current Firmware Sample Contract
  </read_first>
  <files>.planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md</files>
  <action>
    Create the baseline contract artifact with sections for ground truth, SD session metadata, per-sample record fields, file lifecycle, and acceptance rules. Require at least `seq`, `timestamp_us`, and channel payload on every saved sample. Require a session/header record with magic/version, record size, sample rate, channel count/layout, firmware mode/build, filter mode, SD logging mode, stream mode, and start timestamp when available.
  </action>
  <verify>
    <automated>rg -n "seq|timestamp_us|channel|sample rate|channel layout|filter|stream" .planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Artifact states SD is authoritative ground truth
    - Artifact names required per-sample fields
    - Artifact names required session/header metadata
    - Artifact states Open Ephys/serial/TCP/UDP are comparison sinks only
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document the firmware counter and mode-switch contract</name>
  <read_first>
    - `esp32/firmware/main/sd_logger.c`
    - `esp32/firmware/main/open_ephys_stream.c`
    - `esp32/host/stress_test_serial.py`
    - `01-CONTEXT.md` Test-Mode Matrix
  </read_first>
  <files>.planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md</files>
  <action>
    Add a counter contract requiring generated samples, enqueued samples, saved samples, dropped/overflowed samples, stream snapshots/sent samples if available, acquisition-loop overruns, max loop duration, max SD enqueue latency, max SD write latency, and max SD queue depth. Add a mode matrix requiring frequency, SD on/off, filter on/off, stream on/off, and status/counter dump commands or equivalent Kconfig defaults if runtime commands do not exist yet.
  </action>
  <verify>
    <automated>rg -n "generated|saved|dropped|overrun|latency|queue|SD on|filter|stream|frequency|status" .planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Counter list is explicit enough for Phase 2 firmware implementation
    - Mode matrix is explicit enough for Phase 3 stress harness implementation
    - Artifact distinguishes required counters from optional downstream comparison counters
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document current baseline failure layers and verification checklist</name>
  <read_first>
    - `esp32/firmware/main/sd_logger.c`
    - `esp32/firmware/main/open_ephys_stream.c`
    - `esp32/host/analyze_sample_rate.py`
    - `01-RESEARCH.md` Current Loss/Stall Risks
  </read_first>
  <files>.planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md</files>
  <action>
    Add a baseline diagnosis section that names today's known risks: per-sample `fwrite` + `fflush` in the acquisition loop, latest-sample-only `s_latest` stream handoff, and host-only stress analysis lacking SD-file ground truth. Add a minimal verification checklist explaining what Phase 2/3 must prove before the system can claim no sample loss.
  </action>
  <verify>
    <automated>rg -n "fwrite|fflush|s_latest|ground truth|no samples lost|Phase 2|Phase 3" .planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Baseline diagnosis identifies the current synchronous SD write/flush risk
    - Baseline diagnosis identifies the current latest-sample stream overwrite risk
    - Checklist defines minimum evidence for "no samples lost"
    - Artifact clearly defers implementation to later phases
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `rg` checks in each task find the required contract terms.
- `git diff --check -- .planning/phases/01-measurement-contract-baseline/01-BASELINE-CONTRACT.md` reports no whitespace errors.
- Manual code inspection confirms the baseline diagnosis still matches `sd_logger.c` and `open_ephys_stream.c`.
</verification>

<success_criteria>
1. Phase 1 has a committed baseline/contract artifact.
2. SD file record/session metadata requirements are explicit.
3. Firmware counters and mode switches are explicit.
4. Current code risk layers are documented from source inspection.
5. Phase 2 can implement the buffered logger without rediscovering the contract.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-contract-baseline/01-01-SUMMARY.md` when done.
</output>
