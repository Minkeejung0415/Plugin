---
phase: 3
plan: 01
requirements: [TRIG-01, TRIG-02, TRIG-03, OPS-01]
---

<objective>
Wire trigger and Apply Display to atomic joint display config writes.
</objective>

<tasks>
1. Implement `AcqBoardRedPitaya::writeJointDisplayConfig()` (JSON, atomic rename, seq++)
2. Hook `devicethread.cpp` ACQBOARD TRIGGER handler
3. Add Apply Display button in device editor
</tasks>

<verification>
- TRIG-01 trigger writes config
- TRIG-02 atomic + monotonic seq
- TRIG-03 no UDP path changes
- OPS-01 manual apply button
</verification>
