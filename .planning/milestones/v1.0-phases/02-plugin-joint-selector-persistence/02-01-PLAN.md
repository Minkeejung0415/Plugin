---
phase: 2
plan: 01
type: execute
wave: 1
requirements: [JOIN-01, JOIN-02, JOIN-03, JOIN-04]
---

<objective>
Add curated joint multi-select in device editor with XML persistence (max 6 joints).
</objective>

<context>
@.planning/phases/02-plugin-joint-selector-persistence/02-CONTEXT.md
@opensim_joint_catalog.py
@device editor.cpp
</context>

<tasks>

<task id="1">
<title>Shared C++ joint catalog header</title>
<steps>
1. Add `opensim_joint_catalog.h` matching Python catalog entries
2. Expose selection API on `AcqBoardRedPitaya`
</steps>
</task>

<task id="2">
<title>Device editor joint HUD toggles</title>
<steps>
1. Add ToggleButton row for 7 catalog entries
2. Enforce max 6; highlight segments matching active sensors (orange)
3. Wire toggle clicks to board selection state
</steps>
</task>

<task id="3">
<title>XML persistence</title>
<steps>
1. save/load `JOINT_DISPLAY` block via `saveJointDisplayToXml` / `loadJointDisplayFromXml`
2. Restore toggles on plugin settings reload
</steps>
</task>

</tasks>

<verification>
- [ ] JOIN-01: Multi-select visible in Red Pitaya editor
- [ ] JOIN-02: Curated catalog only
- [ ] JOIN-03: Selection survives save/reload XML
- [ ] JOIN-04: Active-segment joints highlighted
</verification>
